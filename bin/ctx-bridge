#!/data/data/com.termux/files/usr/bin/bash
# ctx-bridge: generate session handoff document
STATE="$HOME/projects/openroot/docs/context/state.md"
cat << BRIDGE
# OPENROOT CONTEXT BRIDGE — $(date "+%Y-%m-%d %H:%M")

## IDENTITY
Jesse Ray (jesseray718) — permaculture systems designer, appropriate technology inventor
Device: Samsung Galaxy A15, Termux, Shizuku, no root
Projects: OpenRoot (github.com/jesseray718/openroot), agape-une

## HYPOTHESIS STATUS
$(grep -A2 'H-00' "$STATE" 2>/dev/null | head -20 || echo "Check state.md")

## ACTIVE WORK
$(tail -30 "$STATE" 2>/dev/null || echo "No state.md found")

## TOOLING STATUS
- Voice nanostack: vs (transcribe), vai (AI query), both working via Groq Whisper
- aiq CLI: multi-provider (Groq/Google/Cerebras/OpenRouter/GitHub)
- Local: llama.cpp + Qwen2.5-1.5B on localhost:8080
- kai9000: Alpine sandbox (heavy compute node)
- Git: merge strategy, auto-fixed via git-fix nanobot

## IMMEDIATE NEXT STEPS
1. [Fill in or ask Lumo to suggest from conversation history]

---
Generated by ctx-bridge nanobot
BRIDGE
