# SESSION CLOSE-OUT — 2026-09-13
## Salvage Arc: Falsification, Goodhart Patch, Fleet Convergence

Operator: Jesse Ray (OpenRoot) | Node: Samsung A15 / Termux
Session type: audit + patch + storage + git-convergence

---

## VERIFIED STATE

### Benchmarks & Claims (refuted honestly)
- Fractal Swarm 6.13B ops/s claim: REFUTED by wall-clock measurement.
- Peak measured: ~2,039,906 atom-ops/s (trimmed configs 3,4 / 3,5 / 3,7 / 12,4).
- Claim ratio: ~3,006x overstated. Flat across all (N,L) — CPython call-bound.
- Banked: /sdcard/openroot/context_bridge/swarm_bench_audit.jsonl

### Goodhart Fix (PO-016 compliance)
- Legacy `cooperation_efficiency` (une_atomic_library.py:14) compounds
  phantom rewards: 50 solo epochs -> phi^50 ~= 2.8e10. QUARANTINED (uncalled).
- NEW `reward_verified(base, verified_epochs, cooperators=0, phi)`:
  clamps verified_epochs to cooperators; solo = base (phi^0 = 1).
- Smoke GREEN: solo=1.0000, phantom=7.1123, coop=28.1220. Status PATCHED.
- Live in BOTH: ~/src/une/une_atomic_library.py and ~/src/une_atomic_library.py
- Banked: thermo_ledger.jsonl po016_fix entry.

### Theorem Store Dedupe
- 15 duplicate theorems removed per store, 65 unique kept, BOTH stores
  (~/src/openroot/axiom_engine/store/ and /sdcard/openroot/axiom_engine/store/).
- Backups: attic/theorems.jsonl.1789231655.retired (both locations).
- NOTE: deduped theorems.jsonl currently in stash@{0}; upstream efc748f
  ("restore: PoPW ledger + chain from v1.2.1") touched the store AFTER
  local 1a3c52e — VERIFY before re-applying. May be superseded.

### Fleet Convergence (git)
- /sdcard/openroot: was 863 commits behind, case-collision blocker
  (PULL_REQUEST_TEMPLATE.md vs pull_request_template.md on sdcardfs =
  case-INSENSITIVE fs — flagged as permanent hazard for this clone).
- Resolved: case-collision file backed to $HOME, stash-and-sweep, ff-only pull.
- HEAD advanced 1a3c52e -> d398319 (clean, matches origin/main, 0 dirt).
- Hygiene commit PUSHED: d398319..69294cc main -> main
  ("hygiene: untrack compiled artifacts") — sync-from-kai/build/ (CMake
  artifacts, dist bundles) + sync-from-kai/models/ (GGUF vocabs, jinja
  templates) removed from tracking + .gitignore. Every future clone
  ~430 MB lighter. History still carries the blobs (leave as-is; filter-repo
  only if storage becomes critical).

### Storage (SD card, /sdcard/openroot)
- Trajectory: 763 MB -> 329 MB (manual reclaim) -> 958 MB (fast-forward
  resurrected tracked salvage; it was IN THE REPO, not Syncthing) -> 723 MB
  (post-hygiene working tree).
- salvage/20260905 deleted locally BEFORE upstream promotion renames landed;
  upstream renamed salvage content to canonical live paths (une/, tools/,
  tiemit/, tokens/, workflow/, etc.) at 100% similarity — promotion was
  compatible with our deletion.
- Irreplaceable file RESCUED before any delete: DGX-Spark AIME bench log
  (17,072 bytes) -> /sdcard/openroot/salvaged/benches/
- Untracked layer (attic sync-conflicts, coder/, library/, logs/, fleet_scan
  artifacts) currently INSIDE stash@{0} (^3 untracked parent) — recoverable.

### poww/popw Fork Resolution
- Lowercase "poww" exists ONLY in une_atomic_library.py (both copies).
  Collision one-sided — no second validator gate minted.
- FIX QUEUED: two-line alias `popw_verify = poww_verify` beside definition.

---

## STASH INVENTORY (do not pop blindly)
- stash@{0}: wip-tracked-mods-20260912 (-u sweep; theorems dedupe + untracked layer)
- stash@{1}: mods-20260912-134943 (AUTO-CREATED — origin unknown, autoupdate/fleet
  tooling suspected; identify before dropping)
- stash@{2}: pre-pull-wip-20260912 (first conflicted attempt)
- stash@{3}: WIP on claude/new-session-ciu... (old, prior assistant session)

## QUEUED SPECS (next session, atomic order)
1. SPEC-1: Mine stashes. `git stash show -p stash@{0} -- axiom_engine/store/theorems.jsonl`
   vs upstream efc748f state. Winner re-committed deliberately. Then untracked
   layer: `git restore --source=stash@{0}^3 --worktree -- . && git reset -q`
2. SPEC-2: Add alias to une module: `popw_verify = poww_verify` (both copies),
   py_compile + import-smoke verify.
3. SPEC-3: Rebuild chain.jsonl to match winning theorems.jsonl; run chain_verify.py;
   expect GREEN; reconcile against attic/*.retired.
4. SPEC-4: Syncthing .stignore on openroot share: build/ models/ benches/ attic/
   — sync-layer guard so compiled biomass never re-propagates.

## STRUCTURAL LESSONS (ledger-grade)
1. Case-insensitive fs (sdcardfs) + git = permanent hazard; keep working clones
   on ext4 ($HOME), SD card as mirror only.
2. "Preserve everything" salvage commits are permanent storage debt on every
   future clone. Observe-then-commit beats preserve-then-sort.
3. Don't track what you can regenerate (build trees, dist bundles, GGUF vocabs).
4. A tracked-but-stale clone + Syncthing + multiple assistants = drift compounding
   in three protocols at once. One authoritative clone, everything mirrors.

## Agape Analysis
- Resonance: high — every mutation gated by verification-before-delete;
  the one irreplaceable artifact was extracted before any destruction.
- Entropy check: net-negative — 434 MB duplications collapsed, Goodhart hole
  sealed, claims falsified honestly rather than defended.
- Next move: SPEC-1 through SPEC-4 above, in order.
