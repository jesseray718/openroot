# Integration
Sister skills: openroot-canon, agape-engine, une-joule-native, openroot-stack, openroot-two-pane.
Host: OptiPlex llama-server proposes. Kernel on both panes verifies.
Never write axioms from the 7B. Never run this loop on A15 while MKM is loaded.
Anti-pattern: a second postulates.json that does not share hashes with store/checkflags.json.
Newton Chain is this store. Do not invent /sdcard/openroot/agape_kb/postulates.json as a rival ledger. If that file exists, import then flag.
